#include <stdio.h>

/**
 * main - prints out the output
 * use printf not puts
 * Return: (0)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

